---
- name: Ensure Valkey paths exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    owner: "{{ valkey_uid }}"
    group: "{{ valkey_gid }}"
  with_items:
    - "{{ valkey_base_path }}"
    - "{{ valkey_config_path }}"
    - "{{ valkey_data_path }}"

- name: Ensure Valkey support files installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ valkey_base_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ valkey_uid }}"
    group: "{{ valkey_gid }}"
  with_items:
    - env
  register: valkey_support_files_result

- name: Ensure Valkey configuration file installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ valkey_config_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ valkey_uid }}"
    group: "{{ valkey_gid }}"
  with_items:
    - valkey.conf
  register: valkey_config_file_result

- name: Run if self-building of Valkey container image is not enabled
  when: "not valkey_container_image_self_build | bool"
  block:
    - name: Ensure Valkey container image is pulled via community.docker.docker_image
      when: sysd_docker_image_pull_method == 'ansible-module'
      community.docker.docker_image:
        name: "{{ valkey_container_image }}"
        source: "{{ 'pull' if ansible_version.major > 2 or ansible_version.minor > 7 else omit }}"
        force_source: "{{ valkey_container_image_force_pull if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
        force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else valkey_container_image_force_pull }}"
      register: valkey_container_image_pull_result
      retries: "{{ container_retry }}"
      delay: "{{ container_retry_delay }}"
      until: valkey_container_image_pull_result is not failed

    - name: Ensure Valkey container image is pulled via ansible.builtin.command
      when: sysd_docker_image_pull_method == 'command'
      ansible.builtin.command:
        cmd: "{{ sysd_docker_host_command }} pull {{ valkey_container_image }}"
      register: valkey_container_image_pull_result
      retries: "{{ container_retry }}"
      delay: "{{ container_retry_delay }}"
      until: valkey_container_image_pull_result is not failed
      changed_when: "'Downloaded newer image' in valkey_container_image_pull_result.stdout"

- name: Run if self-building of Valkey container image is enabled
  when: valkey_container_image_self_build | bool
  block:
    - name: Ensure Valkey repository is present on self-build
      ansible.builtin.git:
        repo: "{{ valkey_container_image_self_build_repo }}"
        version: "{{ valkey_container_image_self_build_repo_version }}"
        dest: "{{ valkey_container_image_self_build_src_files_path }}"
        force: "yes"
      register: valkey_git_pull_results

    - name: Ensure Valkey container image is built
      community.docker.docker_image:
        name: "{{ valkey_container_image_self_build_name }}"
        source: build
        force_source: "{{ valkey_git_pull_results.changed if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
        force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else valkey_git_pull_results.changed }}"
        build:
          dockerfile: Dockerfile
          path: "{{ valkey_container_image_self_build_repo_dockerfile_dir }}"
          pull: true
          args:
      register: valkey_container_image_build_result

- name: Ensure Valkey container network is created via community.docker.docker_network
  when: sysd_docker_net_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ sysd_docker_ipv6_enabled }}"
    name: "{{ valkey_container_network }}"
    driver: bridge
    driver_options: "{{ sysd_docker_net_driver_opts }}"
  register: valkey_network_creation_result

- name: Ensure Valkey container network is created via ansible.builtin.command
  when: sysd_docker_net_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ sysd_docker_host_command }} network create
      {% if sysd_docker_ipv6_enabled %}--ipv6{% endif %}
      {{ sysd_docker_net_driver_opts_string }}
      {{ valkey_container_network }}
  register: valkey_network_creation_result
  changed_when: valkey_network_creation_result.rc == 0
  failed_when: valkey_network_creation_result.rc != 0 and 'already exists' not in valkey_network_creation_result.stderr

- name: Ensure Valkey systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/valkey.service.j2"
    dest: "{{ sysd_docker_service_path }}/{{ valkey_identifier }}.service"
    mode: "0644"
  register: valkey_systemd_service_result

- name: Determine whether Valkey needs a restart
  ansible.builtin.set_fact:
    valkey_restart_necessary: >-
      {{
        valkey_support_files_result.changed | default(false)
        or valkey_config_file_result.changed | default(false)
        or valkey_systemd_service_result.changed | default(false)
        or valkey_container_image_pull_result.changed | default(false)
        or valkey_container_image_build_result.changed | default(false)
      }}
